import React from "react";
const Chatcontainer = ({selectedUser,setSelectedUser}) => {
  return (
    <div className="h-full overflow-scroll relative backdrop-blur-lg"></div>
  );
};
export default Chatcontainer;
